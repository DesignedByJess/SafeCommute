{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --omit=dev --prefix backend && npm run build --prefix backend",
    "watchPatterns": ["backend/**"]
  },
  "deploy": {
    "startCommand": "node backend/dist/app.js",
    "healthcheckPath": "/health"
  }
}
